# .github/workflows/ping-edge-function.yml
name: Keep Supabase Edge Function Alive

on:
  schedule:
    - cron: '0 9 */2 * *'  # Every 2 days at 9:00 AM UTC
  workflow_dispatch:       # Manual trigger from Actions tab

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase Edge Function via payload file
        env:
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
        run: |
          echo "🟡 Starting keepalive ping at $(date -u)"
          
          # Write the payload exactly (no stray newline or escapes)
          printf '{"item":"ping","amount":0.01}' > ping-body.json
          
          # DEBUG: Show in logs exactly what we're sending
          echo "📦 Payload:"
          cat ping-body.json
          
          # Curl with file-based JSON
          RESPONSE=$(curl --silent --show-error -X POST https://vwpgkjnncxhklcrqymyr.supabase.co/functions/v1/create-checkout \
            -H "Content-Type: application/json" \
            -H "Origin: https://www.huddlestoncogop.org" \
            -H "Authorization: Bearer $SUPABASE_ANON_KEY" \
            -d @ping-body.json)
          
          echo "✅ Ping response: $RESPONSE"
          if [[ "$RESPONSE" != *"\"status\":\"awake\""* ]]; then
            echo "❌ Unexpected response — failing job"
            exit 1
          fi
